//#[warn(dead_code)] is an attribute used to explicitly enable the dead_code lint, which warns about unused functions, variables, structs, enums, or other code items.
#![allow(dead_code)]

mod problems;

fn main() {}
